---
phase: Scale-Ready Foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "apps/api/src/index.ts",
  "apps/api/src/services/auth.service.ts",
  "apps/api/src/services/audio.service.ts",
  "apps/api/src/services/transcription.service.ts",
  "apps/api/src/controllers/auth.controller.ts",
  "apps/api/src/controllers/audio.controller.ts",
  "apps/api/src/routes/auth.routes.ts",
  "apps/api/src/routes/audio.routes.ts",
  "apps/api/src/middleware/auth.middleware.ts",
  "apps/api/src/lib/socket.ts",
  "apps/api/src/lib/ai.ts"
]
autonomous: true
requirements: [API-01, API-02, API-03]

must_haves:
  truths:
    - "API is split into routes, controllers, and services"
    - "Auth middleware works as a standalone module"
    - "Transcription orchestration lives in a dedicated service"
  artifacts:
    - path: "apps/api/src/routes/auth.routes.ts"
      provides: "Auth routing"
    - path: "apps/api/src/services/transcription.service.ts"
      provides: "Core transcription logic"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "apps/api/src/routes/auth.routes.ts"
      via: "app.use()"
---

<objective>
Refactor the monolithic index.ts into a clean, modular architecture (Routes, Controllers, Services).

Purpose: Establish a robust foundation for Phase 2 (Queuing) and handle technical debt.
Output: Decoupled API logic with standardized services and unified AI/Socket libraries.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure and shared libraries</name>
  <files>
    apps/api/src/lib/socket.ts,
    apps/api/src/lib/ai.ts
  </files>
  <action>
    Create src/services, src/controllers, src/routes, src/middleware, src/lib.
    In src/lib/socket.ts: Initialize and export the Socket.io Server (io).
    In src/lib/ai.ts: Initialize and export Replicate and Anthropic clients.
  </action>
  <verify>Files exist and export expected instances.</verify>
  <done>Infrastructure libs created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Auth and Middleware modules</name>
  <files>
    apps/api/src/middleware/auth.middleware.ts,
    apps/api/src/services/auth.service.ts,
    apps/api/src/controllers/auth.controller.ts,
    apps/api/src/routes/auth.routes.ts
  </files>
  <action>
    Port the authenticate middleware to auth.middleware.ts.
    Port register/login logic to auth.service.ts and auth.controller.ts.
    Define auth routes in auth.routes.ts.
  </action>
  <verify>Auth flow (POST /auth/login) works after temporary wiring.</verify>
  <done>Auth layer modularized.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Transcription Service and Modular API wiring</name>
  <files>
    apps/api/src/services/transcription.service.ts,
    apps/api/src/controllers/transcription.controller.ts,
    apps/api/src/routes/transcription.routes.ts,
    apps/api/src/index.ts
  </files>
  <action>
    Move transcribeAsync and applyStyleGuideDirect to transcription.service.ts.
    Implement TranscriptionController to handle /transcribe and /transcripts requests.
    Wire all routers (auth, audio, transcription) into index.ts.
    Cleanup index.ts to only contain app initialization, middleware, and server start logic.
  </action>
  <verify>Transcription flow (POST /transcribe) successfully triggers the service logic.</verify>
  <done>API fully modularized.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds (no type errors in the new structure)
- [ ] `GET /health` returns status:ok
- [ ] `POST /auth/login` returns valid JWT
</verification>

<success_criteria>
- All tasks completed
- index.ts is reduced by >60% in length
- No regression in core functional flows
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-01-SUMMARY.md`
</output>
