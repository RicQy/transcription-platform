---
phase: Advanced Speaker Management
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  "apps/web/src/api/speakers.ts",
  "apps/web/src/components/SpeakerLabeler.tsx",
  "apps/web/src/pages/TranscriptionView.tsx"
]
autonomous: false
requirements: [SPEAK-01, SPEAK-02, REQUIRE-04]
user_setup: []

must_haves:
  truths:
    - "UI allows selecting a verified speaker for a diarization label"
    - "Changes are persisted to the backend immediately"
  artifacts:
    - path: "apps/web/src/components/SpeakerLabeler.tsx"
      provides: "Speaker management component"
---

<objective>
Implement the Speaker Labeling UI in the frontend.

Purpose: Provide legal professionals with an interface to drag-and-drop or select identities for speakers.
Output: SpeakerLabeler component and integration into the main transcription view.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api/src/routes/speaker.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Speaker API Service in Frontend</name>
  <files>apps/web/src/api/speakers.ts</files>
  <action>
    Implement API client functions for fetching and updating speaker mappings.
  </action>
  <verify>lint check on speakers.ts</verify>
  <done>Frontend API client ready.</done>
</task>

<task type="auto">
  <name>Task 2: Build SpeakerLabeler Component</name>
  <files>apps/web/src/components/SpeakerLabeler.tsx</files>
  <action>
    Create a component that lists diarization labels and allows selection of a verified speaker from a dropdown.
    Support creating a "New Speaker" inline.
  </action>
  <verify>Component renders with sample data.</verify>
  <done>SpeakerLabeler component built.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into Transcription View</name>
  <files>apps/web/src/pages/TranscriptionView.tsx</files>
  <action>
    Add the SpeakerLabeler to the sidebar or a dedicated tab in the TranscriptionView.
    Refresh the transcript view when labels are updated.
  </action>
  <verify>Selecting a speaker in the labeler updates the transcript speaker names.</verify>
  <done>UI integrated.</done>
</task>

</tasks>

<verification>
- [ ] Speaker list is fetched correctly
- [ ] Linking a speaker persists to DB
- [ ] UI reflects changes in real-time
</verification>

<success_criteria>
- Minimal clicks to identify a speaker
- Clear feedback during saving
</success_criteria>

<output>
After completion, create `.planning/phases/phase-2/02-02-SUMMARY.md`
</output>
