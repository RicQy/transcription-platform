---
phase: Gold Standard Compliance
plan: 01
type: execute
wave: 1
depends_on: ["02-03"]
files_modified: [
  "apps/api/src/schema.sql",
  "apps/api/src/services/evaluation.service.ts",
  "apps/api/src/controllers/evaluation.controller.ts",
  "apps/api/src/routes/evaluation.routes.ts",
  "apps/api/src/index.ts"
]
autonomous: true
requirements: [ACC-01, ACC-02]
user_setup: []

must_haves:
  truths:
    - "Evaluation engine calculates WER and Character Error Rate (CER)"
    - "System identifies exact line-by-line CVL violations vs Gold Standard"
  artifacts:
    - path: "apps/api/src/services/evaluation.service.ts"
      provides: "Transcription accuracy metrics engine"
---

<objective>
Implement the Automated Evaluation Engine.

Purpose: Quantify the "Perfection" goal by measuring AI output against human-verified Gold Standard transcripts.
Output: Evaluation service and database records for tracking accuracy trends.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@apps/api/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Evaluation Schema</name>
  <files>apps/api/src/schema.sql</files>
  <action>
    Add 'evaluations' table (id, transcript_id, gold_standard_text, wer, cer, alignment_data).
    Run database migration.
  </action>
  <verify>check_tables.ts shows 'evaluations'.</verify>
  <done>Schema updated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Evaluation Service (WER/CER)</name>
  <files>apps/api/src/services/evaluation.service.ts</files>
  <action>
    Implement Levenshtein-based WER/CER calculation.
    Store results in the evaluations table.
  </action>
  <done>Evaluation engine core ready.</done>
</task>

<task type="auto">
  <name>Task 3: Implement API Endpoints</name>
  <files>
    apps/api/src/controllers/evaluation.controller.ts,
    apps/api/src/routes/evaluation.routes.ts
  </files>
  <action>
    Expose POST /transcripts/:id/evaluate.
    Return accuracy metrics and diff alignment.
  </action>
  <done>Endpoints wired.</done>
</task>

</tasks>

<verification>
- [ ] WER calculation is accurate for basic samples
- [ ] Evaluation data is persisted
</verification>

<success_criteria>
- Sub-second evaluation of 1hr transcripts
- Clear structural diff data for the UI
</success_criteria>

<output>
After completion, create `.planning/phases/phase-3/03-01-SUMMARY.md`
</output>
