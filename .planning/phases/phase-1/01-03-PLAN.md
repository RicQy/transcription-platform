---
phase: Scale-Ready Foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [
  "apps/api/src/lib/queue.ts",
  "apps/api/src/workers/transcription.worker.ts",
  "apps/api/src/services/transcription.service.ts",
  "apps/api/src/index.ts"
]
autonomous: true
requirements: [INFRA-03]
user_setup:
  - service: Redis
    why: "Message broker for BullMQ background processing"
    env_vars:
      - name: REDIS_URL

must_haves:
  truths:
    - "Transcription tasks are offloaded to BullMQ workers"
    - "Socket.io events are correctly emitted from worker contexts"
  artifacts:
    - path: "apps/api/src/lib/queue.ts"
      provides: "BullMQ connection and queue instance"
---

<objective>
Implement BullMQ + Redis for stable, asynchronous background processing.

Purpose: Prevent API request timeouts and handle long-running transcription tasks reliably.
Output: Queue management system, background worker, and task orchestration logic.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api/src/services/transcription.service.ts
@apps/api/src/lib/socket.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize BullMQ and Redis</name>
  <files>apps/api/src/lib/queue.ts</files>
  <action>
    Create a shared Redis connection and initialize the "transcription" queue using BullMQ.
  </action>
  <verify>tsc check on lib/queue.ts</verify>
  <done>Queue instance available.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Background Worker</name>
  <files>apps/api/src/workers/transcription.worker.ts</files>
  <action>
    Port the transcription logic from TranscriptionService.processTranscription into a BullMQ Worker.
    Ensure Socket.io emissions still work (via getSocket()).
  </action>
  <verify>Worker correctly processes a test job (mocked ASR).</verify>
  <done>Background worker active.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Transcription Service</name>
  <files>apps/api/src/services/transcription.service.ts</files>
  <action>
    Modify the service to push jobs to the queue instead of running them in the background via fire-and-forget.
  </action>
  <verify>API /transcribe returns 202 and a job is added to Redis.</verify>
  <done>Service refactored.</done>
</task>

</tasks>

<verification>
- [ ] Redis connection is stable
- [ ] Worker starts without errors
- [ ] End-to-end transcription flow works (enqueued -> processed -> finished event)
</verification>

<success_criteria>
- No more fire-and-forget loops in the API process
- Retries and priority support ready in BullMQ
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-03-SUMMARY.md`
</output>
