---
phase: Scale-Ready Foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "apps/api/src/lib/storage.ts",
  "apps/api/src/services/audio.service.ts",
  "apps/api/src/controllers/audio.controller.ts",
  "apps/api/src/routes/audio.routes.ts",
  ".env.example"
]
autonomous: false
requirements: [INFRA-01, INFRA-02]
user_setup:
  - service: Cloudflare R2 / AWS S3
    why: "Object storage is required for production-scale audio handling"
    env_vars:
      - name: STORAGE_BUCKET
      - name: STORAGE_REGION
      - name: STORAGE_ENDPOINT
      - name: STORAGE_ACCESS_KEY_ID
      - name: STORAGE_SECRET_ACCESS_KEY

must_haves:
  truths:
    - "API can generate pre-signed PUT URLs for direct uploads"
    - "Audio metadata stores S3 keys instead of local paths"
  artifacts:
    - path: "apps/api/src/lib/storage.ts"
      provides: "S3/R2 Client wrapper"
---

<objective>
Implement S3/R2 object storage integration and Pre-signed URL generation.

Purpose: Enable direct-to-storage uploads, bypassing API memory bottlenecks.
Output: Storage utility library, updated Audio metadata logic, and pre-signed URL endpoint.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
@~/.gemini/antigravity/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api/src/services/audio.service.ts
@apps/api/src/controllers/audio.controller.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Select Storage Provider</decision>
  <context>We need to decide between AWS S3 or Cloudflare R2. R2 is zero-egress and highly compatible with the S3 API.</context>
  <options>
    <option id="r2"><name>Cloudflare R2</name><pros>Zero egress fees, easy setup, S3 compatible</pros><cons>Requires Cloudflare account</cons></option>
    <option id="s3"><name>AWS S3</name><pros>Industry standard, deep integration options</pros><cons>Egress fees can be high for large audio files</cons></option>
  </options>
  <resume-signal>Select: r2 or s3</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create storage utility library</name>
  <files>apps/api/src/lib/storage.ts</files>
  <action>
    Implement StorageService using @aws-sdk/client-s3 and @aws-sdk/s3-request-presigner.
    Implement getPreSignedUploadUrl(key: string, contentType: string) method.
  </action>
  <verify>tsc check on lib/storage.ts</verify>
  <done>Storage library exists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Pre-signed URL endpoint and update Audio metadata</name>
  <files>
    apps/api/src/controllers/audio.controller.ts,
    apps/api/src/routes/audio.routes.ts,
    apps/api/src/services/audio.service.ts
  </files>
  <action>
    Add GET /audio/upload-url to audio.routes.ts.
    Update AudioController to generate a unique key (UUID) and return a pre-signed URL.
    Update AudioService.saveFile to store the S3 key/url in the database.
  </action>
  <verify>API endpoint returns a URL matching S3 format.</verify>
  <done>Upload URL workflow implemented.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] `GET /audio/upload-url?filename=test.mp3&contentType=audio/mpeg` returns a 200 with URL
</verification>

<success_criteria>
- All tasks completed
- Secret keys are correctly loaded from Env
- Pre-signed URLs have short expiration (15-30m)
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-02-SUMMARY.md`
</output>
